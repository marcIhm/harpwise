OVERVIEW

  This mode is useful only for the maintainer or developer of
  harpwise; it is therefore not listed in the general usage
  information.

  The required additional argument specifies a task needed; it can be
  one of:

<%= get_extra_desc(for_usage: true).join("\n") %>
  
